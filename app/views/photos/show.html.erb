<p id="notice"><%= notice %></p>

<p>
  <%= image_tag @photo.image.url, class: 'img-fluid' %>
  <!-- <%#=  @photo.image_data %> -->
</p>

<p>
  <strong><%= @photo.user.email %></strong>
          <%= @photo.caption %>
</p>

<h3>Create New Comment:</h3>

<%= link_to 'New Photo Comment', new_photo_comment_path(@photo) %>
|
<% if current_user == @photo.user %>
    <!-- <%#= link_to "Cancel", photos_path %> | -->
    <%= link_to 'Edit', edit_photo_path(@photo) %> |
    <%= link_to 'Destroy', @photo, method: :delete, data: { confirm: 'Are you sure?'} %>
<% end %>
<br><br>
<%= link_to 'Back to Photos', photos_path %>
|
<%= link_to 'Back to Profile', profile_path %>

<br><br>

<h3>Existing Comments:</h3>

<%= render 'shared/photo_comments', locals: {photo: @photo, photo_comments: @photo_comments } %>
